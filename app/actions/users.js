import {USER_EMPTY, USER_UPDATE} from '../constants/users'
import fetchival from 'fetchival'
import _ from 'lodash'

import *

export defalt

const update = (data) => ({
  type: USER_UPDATE,
  data
})

export const empty = () => ({
  type: USER_EMPTY
})
